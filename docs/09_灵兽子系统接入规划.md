# 灵兽重构：子系统接入规划

> 文档版本：v1.0 ｜ 状态：待实施 ｜ 前置阅读：[灵兽功能设计文档](08_灵兽功能设计文档.md)

## 1. 结论与实施原则

现有 P1 灵兽应**渐进式重构，不删除重做**。保留 `data_spirit_beast`、`user_spirit_beast`、`user_spirit_beast_capture` 及玩家实例，新增成长、技能、材料、审计和快照版本字段；先让“成长面板 + 现有灵契”上线，再接自动技能，最后接经济与长线内容。

实现必须遵循：

1. 数据迁移先于代码发布，所有 DDL 可重复执行。
2. 任何扣材料与产出均以事务、行锁和唯一业务键处理。
3. 战斗读取 `snapshot.v2`，旧会话继续按 `snapshot.v1` 结算。
4. 服务层管理规则，`output_main.py` 只做命令路由，Markdown 只做展示。
5. 每个阶段独立可回滚；不以删除玩家数据作为回滚方式。

## 2. 当前接入点盘点

| 子系统 | 当前文件/数据 | 现状 | 重构动作 |
| --- | --- | --- | --- |
| 灵兽领域逻辑 | `Game_main/g12_spirit_beast.py` | 寻访、出战、基础灵契、本源协同 | 拆分查询、养成、技能、快照、展示服务 |
| 命令与 Webhook | `output_main.py` | 已路由 `灵兽/图鉴/寻访/出战` | 追加指令白名单和路由；参数按紧凑文本兼容解析 |
| 菜单 | `Game_main/g0_menu.py` | 灵兽菜单与入口存在 | 改为总览、培养、图鉴、仙师四页 |
| 单人战斗 | `Game_main/g6_dungeon.py` + `battle_session` | 开战时写入基础灵兽快照并注入 Buff | 消费 v2 成长/技能快照，结算羁绊流水 |
| 队伍战斗 | `Game_main/g17_party_battle.py` + `party_battle_session` | 每位成员有灵兽快照 | 与单人共用效果适配器和事件协议 |
| 战斗内核 | `Tool/combat_system.py` | 已有 Buff、治疗、灼烧等白名单 | 增加灵兽事件钩子和硬上限，不解析任意配置 |
| 洞府 | `Game_main/g14_estate.py` | 灵兽园只影响容量 | 灵兽园增加伴兽草日常产出和新容量曲线 |
| 商城/物品 | `Game_main/g10_shop.py`、`user_item` | 支持商品和限购 | 加入周限购伴兽草/口粮，不上架强度直充材料 |
| 数据库 | `数据库源文件/p1_spirit_beast.sql` | 4 模板、单资质、羁绊经验 | 新建 P2 迁移脚本，保持 P1 兼容 |

## 3. 目标模块划分

建议新增 `Game_domain/spirit_beast/`，由 `g12_spirit_beast.py` 保留为兼容命令 Facade；不要让命令文件继续承载事务与战斗规则。

```text
Game_domain/spirit_beast/
  catalog.py          # 模板、图鉴、解锁条件
  repository.py       # 仅 SQL 读写、FOR UPDATE、映射
  service.py          # 寻访/合成/喂养/资质/血脉/炼化/还原事务
  skill_rules.py      # 结构化技能白名单、数值 clamp、触发优先级
  snapshot.py         # v1/v2 序列化、兼容与冻结
  battle_adapter.py   # 事件转为 CombatEntity 的 Buff/治疗/伤害
  presentation.py     # Markdown 分页与文本指令构建
Game_main/g12_spirit_beast.py  # 兼容导出与 @reg_xz_func 命令入口
```

`repository.py` 不产生业务结果；`service.py` 不拼 Markdown；`presentation.py` 不访问数据库。这一层次可避免将来扩展内丹或灵兽阵位时再次把规则散落到路由层。

## 4. 数据模型与迁移

### 4.1 新增/扩展表

新建 `数据库源文件/p2_spirit_beast_rebuild.sql`。建表使用 `CREATE TABLE IF NOT EXISTS`，为旧列补充使用 `information_schema` + 动态 `ALTER`，并把每项数据迁移写成可幂等 `INSERT ... ON DUPLICATE KEY UPDATE`。

| 表/字段 | 用途 | 关键约束/索引 |
| --- | --- | --- |
| `data_spirit_beast` 扩展 `template_version, innate_skill_code, unlock_rule` | 七只模板与固有技能 | 模板 ID 不变；`skill_code` 白名单 |
| `user_spirit_beast` 扩展 `realm, realm_exp, hp_aptitude, atk_aptitude, def_aptitude, bloodline_tier, bloodline_stage, is_locked, schema_version` | 玩家主状态 | `uid,is_active` 索引；单用户一个出战由事务保证 |
| `user_spirit_beast_skill` | 技能槽、等级、装备状态 | `UNIQUE(beast_instance_id, slot_no)` |
| `user_spirit_beast_material_ledger` | 材料消耗/返还审计 | `UNIQUE(uid, business_key, operation)` |
| `user_spirit_beast_refine` | 炼化记录 | `UNIQUE(beast_instance_id)`，防止二次炼化 |
| `user_spirit_beast_bond_settlement` | 战斗羁绊经验结算 | `UNIQUE(battle_id, beast_instance_id)` |
| `user_spirit_beast_daily` | 寻访、口粮软上限等日状态 | `PRIMARY KEY(uid, stat_date)` |
| `data_spirit_beast_skill` | 技能白名单配置与数值版本 | `UNIQUE(skill_code, version)` |
| `data_spirit_beast_exchange` | 兽魄兑换配置 | `UNIQUE(exchange_code)` |

### 4.2 P1 数据回填

| 旧字段 | 新字段 | 回填规则 |
| --- | --- | --- |
| `aptitude (70–95)` | 三维资质 | `hp=aptitude`，`atk=aptitude`，`def=aptitude`；不让既有灵兽变弱 |
| `bond_exp` | `bond_exp` | 原样保留，按新阈值换算羁绊级；溢出保留 |
| `is_active` | `is_active` | 原样保留；若异常多只出战，保留最早获得的一只并记录告警 |
| `beast_id` | 模板与固有技能 | 按现有四只模板映射；无模板实例隔离为“待修复”且不删除 |
| `user_spirit_beast_capture` | `user_spirit_beast_daily.seek_count` | 保留当日寻访限制；历史表不删，至少保留一个版本周期 |

迁移前必须执行只读统计：实例总数、按模板分布、每 UID 出战数、孤儿模板数、容量超额数。迁移后将同一统计写入发布记录并比较；任一实例数量下降即停止发布。

### 4.3 事务范式

以“灵兽资质”操作为例：

1. `SELECT user_spirit_beast ... FOR UPDATE`，校验 UID、锁定状态、当前战斗状态。
2. `SELECT user_item ... FOR UPDATE`，校验饲灵丹数量与资质上限。
3. 计算确定性结果与业务键 `beast-aptitude:{uid}:{beast_id}:{request_id}`。
4. 插入材料流水（唯一键冲突直接返回先前结果）。
5. 扣物品、更新灵兽、写审计，`COMMIT`。

炼化、还原、上阵、技能装备都采用同一模式。不要依赖内存锁，也不要在提交后再补写流水。

## 5. 战斗接入协议

### 5.1 快照版本

```json
{
  "schema_version": 2,
  "instance_id": 123,
  "template_id": 1,
  "template_version": 1,
  "name": "赤焰灵狐",
  "role": "STRIKER",
  "stats": {"realm": 12, "hp_aptitude": 70, "atk_aptitude": 84, "def_aptitude": 66},
  "bloodline": {"tier": "灵品", "stage": 3},
  "effects": [{"code": "FOX_FIRE_CHASE", "level": 2, "remaining": 1}],
  "rng_seed": "server-generated",
  "event_counts": {},
  "bond_award_version": 1
}
```

- `snapshot.v1`：沿用当前 `combat_bonus` / `synergy` 读取和结算，不试图在线补字段。
- `snapshot.v2`：进入 `battle_adapter`，先计算并 clamp 所有静态 Buff，再注册结构化事件效果。
- `template_version`：以后调数值只影响新开战斗；旧快照可重放。

### 5.2 统一事件接口

单人和队伍战斗都通过以下有限事件调用适配器：`BATTLE_START`、`ROUND_START`、`OWNER_SKILL_CAST`、`OWNER_HP_BELOW`、`ENEMY_SHIELD_BROKEN`、`ROUND_END`、`BATTLE_END`。

适配器返回 `BattleEffect[]`（加 Buff、治疗、伤害、净化、法力），再由 `Tool/combat_system.py` 执行。事件输入和返回均为数据对象；禁止 `eval`、反射调用或从数据库导入可执行代码。

### 5.3 结算与故障恢复

- 开战：与创建战斗会话同事务冻结灵兽快照。
- 进行中：每次保存会话将剩余触发次数、事件计数写回快照。
- 结束：以 `battle_uuid`/队伍 session ID 幂等结算羁绊经验；失败、放弃、超时可给予较低羁绊经验，避免刷失败。
- 重试：已存在结算流水时返回记录结果；任何异常仅回滚当前事务，不清理玩家灵兽。

## 6. 命令、菜单与 Webhook 接入

### 6.1 路由变更

在 `output_main.py` 的无后缀/有后缀命令白名单和 `if/elif` 分发中加入文档 08 列出的灵兽指令。复杂参数统一传入 `parse_spirit_beast_command()`，不要在路由层 `split()` 后直接写库。

特别注意：当前入口会清洗空白字符。因此下面两种输入必须等价：

```text
灵兽资质 123 攻击 2
灵兽资质123攻击2
```

固定语义字段（“气血/攻击/防御”“确认还原”等）应以白名单 token 或前缀匹配解析；编号以正则提取并校验范围。不要把空格作为唯一分隔符。

### 6.2 页面拆分

| 页 | 入口 | 必显信息 | 主操作 |
| --- | --- | --- | --- |
| 灵兽总览 | `灵兽` | 出战、战斗贡献、培养提示、容量 | 详情、寻访、仙师 |
| 详情 | `灵兽详情 ID` | 三维资质、境界、血脉、技能、下一成本 | 喂养、资质、血脉、技能、出战 |
| 图鉴 | `灵兽图鉴 页码` | 定位、元素、来源、解锁 | 合成/寻访 |
| 仙师 | `灵兽仙师` | 兽魄余额、可兑换物、材料来源 | 定向兑换 |

`show` 仅写玩家易懂的操作名称，`text` 写完整命令。任何 Markdown 页也必须附“返回灵兽”和对应纯文本用法。

## 7. 分期实施与发布闸门

| 阶段 | 交付 | 涉及范围 | 发布闸门 |
| --- | --- | --- | --- |
| P0：基线 | 数据体检、迁移脚本、P1 回填演练、快照 v1 兼容测试 | SQL、测试、发布手册 | 测试库实例数和出战数 100% 一致 |
| P1：可见成长 | 灵兽详情、境界/三资质、锁定、总览分页 | domain、g12、g0、路由 | 并发喂养/资质扣物品不重复；旧指令可用 |
| P2：经济闭环 | 副本掉落、灵兽园伴兽草、炼化、仙师兑换、坊市限购 | dungeon、estate、shop、item ledger | 7 日资源模拟不出现负库存或无来源材料 |
| P3：战斗技能 | v2 快照、事件适配器、单人/队伍技能、战报 | combat、g6、g17、battle tables | 重启恢复、重复行动、超时结算结果一致 |
| P4：血脉与体验 | 血脉、重置、图鉴七只、运营统计 | service、菜单、GM/统计 | 强度上限压测符合设计文档 |
| P5：可选长线 | 内丹/共鸣或阵位扩展 | 新模块 | 另立设计评审，不与 P1–P4 捆绑 |

每阶段完成后再开启下一阶段，不允许先把 P3 代码发布而让未迁移玩家遇到快照版本错误。

## 8. 测试矩阵

新增测试建议按 `tests/test_spirit_beast_*.py` 拆分；现有 `tests/test_spirit_beast.py` 保留作为 v1 回归。

| 类型 | 必测场景 |
| --- | --- |
| 纯规则单测 | 三资质上限、保底、血脉阶级、贡献 clamp、技能触发次数、v1/v2 快照反序列化 |
| Repository/事务 | 非本人操作、锁定/出战炼化拒绝、材料不足、重复 request ID、并发上阵只剩一只 |
| 命令解析 | 有空格/无空格、全角符号、缺参数、超长输入、错误编号、确认口令 |
| 单人战斗 | 开战冻结、灵兽战报、服务重启恢复、结束羁绊只发一次 |
| 队伍战斗 | 每位成员独立灵兽、断线/超时、事件顺序、队伍与单人同技能效果一致 |
| 迁移 | 空库、P1 正常库、同 UID 多出战脏数据、孤儿模板、重复运行迁移 |
| 回归 | `python -m unittest discover -s tests` 全绿；商城、洞府、副本、技能融合和装备熔炼不回归 |

建议在 P3 前增加固定随机种子的战斗 Golden Test：输入同一快照、同一指令序列，断言战报与最终状态完全相同。

## 9. 发布、观测与回滚

### 9.1 发布前

1. 备份相关表与当前 `battle_session`/`party_battle_session`。
2. 在 staging 执行 P2 迁移两次，确认第二次无数据变化。
3. 运行全量测试和至少 100 次并发模拟：喂养、炼化、上阵、战斗结算。
4. 生产先发布可读取 v1/v2 的代码，再执行迁移并打开 P1 开关。

### 9.2 观测

日志采用结构化事件：`spirit_beast.operation`、`spirit_beast.snapshot_version`、`spirit_beast.trigger`、`spirit_beast.settlement`。每条包含 `uid`、`beast_instance_id`、`business_key`、`battle_id`（如有）和结果码，不写入玩家昵称、消息原文或密钥。

告警条件：迁移实例数减少、灵兽效果异常超过角色战力上限、材料出现负数、同一战斗重复结算、v2 快照反序列化失败。

### 9.3 回滚

- P1/P2：关闭写入开关，代码回退为只读 P1 面板；新增列/表保留，不删除数据。
- P3：停止新建 v2 战斗；已开战的 v2 会话由兼容代码结算，不能回退到 v1 中途重算。
- 数据修复：只可通过迁移脚本/GM 审计业务键补偿，不执行“删表重建”或批量删除玩家灵兽。

## 10. 实施清单

- [ ] 完成 `p2_spirit_beast_rebuild.sql` 与 rollback 说明。
- [ ] 建立领域模块和 P1 Facade 兼容层。
- [ ] 先上线详情/成长/锁定与纯文本解析兼容。
- [ ] 接入灵兽园产出、材料物品、炼化与兑换流水。
- [ ] 建立 v2 快照和战斗事件适配器，完成单人再完成队伍。
- [ ] 补齐迁移、并发、重启恢复、全量回归测试。
- [ ] 灰度观测 7 天后，再开放血脉和技能材料的完整产出。
