# 灵兽、队伍与三千道途攻略

- `灵兽图鉴` 查看可捕捉灵兽；满足副本掉落条件后使用 `灵兽寻访`，再用 `灵兽出战 编号` 激活加成。
- 群聊中使用 `队伍创建`，把队伍码交给同群成员，用 `队伍加入 队伍码` 加入。队长可用 `布阵 阵法-位置` 调整站位，全员发送 `队伍准备`。
- `道途开启` 由队长发起三千道途，成员用 `道途投票 选项` 决定分支；`队伍战斗` 开始协作战斗，成员发送 `队伍战斗行动 普攻/防御/调息`。

队伍与三千道途只在群聊开放，避免跨群成员和奖励归属混乱。
