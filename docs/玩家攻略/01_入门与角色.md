# 入门与角色攻略

1. 发送 `注册游戏 名称`，再用 `选择角色 角色名` 获得首位角色。
2. 用 `角色背包` 查看角色，发送 `出战 角色编号` 设定当前角色；`当前角色` 查看属性，`收回` 取消出战。
3. `参悟` 开始修炼，完成后发送 `领取参悟经验`。等级到阶段上限、经验足够时用 `悟道进阶`，系统会消耗对应破境丹。
4. `查看本源` 查看本源，`本源升级` 消耗材料提升属性。优先在副本、药园和炼丹中积累材料。

提示：角色属性、装备与本源都会即时影响当前出战角色的战力。
