# 指令按钮格式修改报告（qqbot-cmd-input → qqbot-cmd-enter）

修改时间：2026-07-24

## 一、修改依据（QQ 机器人官方文档）

官方文档（https://bot.q.qq.com/wiki/develop/api-v2/server-inter/message/trans/text-chain.html ）中"指令操作"定义了两种格式：

| 格式 | 写法 | 行为 | 属性 |
|------|------|------|------|
| 回车指令 | `<qqbot-cmd-enter text="xxx" />` | 点击后**直接发送** text | 仅支持 `text`（必填，≤100字符） |
| 参数指令 | `<qqbot-cmd-input text="xxx" show="xxx" />` | 点击后 text **插入输入框**，用户自行编辑发送 | `text` 必填、`show` 选填、`reference` 选填 |

> ⚠️ 官方限制：**回车指令在群聊和文字子频道不支持，仅单聊（C2C）可用**。本项目同时服务群聊与单聊，群聊场景下 enter 按钮不可点击，用户手动输入指令不受影响。

## 二、修改规则

依据 `output_main.py` 的指令解析逻辑（`wuhouzhui` 无后缀指令 / `youhouzhui` 带后缀指令及各指令空参行为）：

**改为 `qqbot-cmd-enter`（点击直接发送）：**
1. 无后缀指令：主菜单、各系统菜单、参悟、当前角色、收回、查看本源、放弃副本、一键采摘、一键收丹等
2. 空参有默认行为的指令：各类背包/列表（默认第1页）、种子商店、丹方列表、战力排行、挑战副本（空参显示可挑战列表）
3. 参数已填完整的指令：`角色介绍 萧炎`、`挑战副本 5`、`采摘 2`、`装备详情 10001` 等

**保留 `qqbot-cmd-input`（需玩家输入参数）：**
1. `text` 以空格结尾（预留参数位）：`选择角色 `、`播种 `、`炼丹 ` 等
2. `show` 带 `*` 或参数示例：`注册游戏*`、`挑战副本 1`、`跳转【页数】` 等
3. 空参会报"指令错误"的必须带参指令（裸指令按钮）：注册游戏、选择角色、角色介绍、角色属性、出战、物品信息、副本信息、挑战怪物、激活技能、卷轴信息、技能信息、技能装备、技能卸下、穿戴装备、卸下装备、强化装备、装备详情、出售装备、购买种子、播种、一键播种、采摘、解锁药田、施肥、出售药材、炼丹、收丹、服丹、解锁丹炉、加速炼丹、添火

**格式说明：** 由于 `qqbot-cmd-enter` 官方仅支持 `text` 属性，改造后按钮显示文字统一为指令本身（原 `show` 中的 emoji/自定义文案不再显示，如 `⬅️ 返回主菜单` 显示为 `/主菜单`）。

## 三、各文件修改明细

### 1. `Game_main/g0_menu.py`（菜单系统，39 处改 enter）

| 位置 | 改为 enter 的指令 | 保留 input 的指令 |
|------|------|------|
| 主菜单 show_main_menu (197-201) | 角色菜单/参悟菜单/本源菜单/技能菜单/副本菜单/装备菜单/药园菜单/炼丹菜单/战力菜单/玩法介绍 | 注册游戏*（180行） |
| 角色菜单 (227-249) | 当前角色、悟道进阶、收回、主菜单 | 选择角色、角色介绍、角色背包 页码、角色属性、出战、物品背包 页码、物品信息 |
| 参悟菜单 (274-282) | 参悟、参悟状态、领取参悟经验、主菜单 | — |
| 本源菜单 (307-315) | 查看本源、本源升级、本源技能、主菜单 | — |
| 技能菜单 (338-352) | 主菜单 | 激活技能、技能背包(尾空格)、技能信息、卷轴信息、技能装备、技能卸下 |
| 装备菜单 (369-385) | 当前装备、主菜单 | 装备背包 页码、穿戴装备、卸下装备、强化装备、装备详情、出售装备 |
| 副本菜单 (411-425) | 副本列表、怪物列表、放弃副本、主菜单 | 副本信息、挑战副本、挑战怪物 |
| 药园菜单 (449-473) | 药园、种子商店、种子背包、一键采摘、主菜单 | 购买种子、播种、一键播种、采摘、施肥、解锁药田、出售药材 |
| 炼丹菜单 (497-519) | 查看丹炉、丹方列表、一键收丹、添火次数、主菜单 | 炼丹、收丹、服丹、解锁丹炉、加速炼丹、添火 |
| 战力菜单 (544-552) | 我的战力、战力排行、战力排行 全服、主菜单 | — |
| 玩法介绍 (649) | 主菜单、帮助 | — |

### 2. `Game_main/g1_role.py`（角色系统，21 处改 enter）

- 注册流程 (21/25行)：`主菜单 ` → enter（`注册游戏` 需输名称，保留 input）
- 注册成功/选角页 (37-42, 64-69行)：`角色介绍 萧炎/王林/韩立/石昊/叶凡/孟川` ×12 → enter（参数已填完整）
- 已选角提示 (81-82行)：主菜单、查看本源、副本列表、参悟 → enter
- 角色介绍页 (169行)：`选择角色 {role_name}` → enter
- 角色背包 (551, 563行)：`角色属性 {role_id}` → enter；上一页/下一页 `角色背包 {n}` → enter（`跳转【页数】` 保留 input）
- 物品背包 (613, 624行)：`物品信息 {item_name}` → enter；翻页同上
- 保留 input：17/47/71行 `选择角色 `/`角色介绍 `（尾空格+*）、563/624行 `跳转【页数】`

### 3. `Game_main/g5_skill.py`（技能系统，2 处改 enter）

- 423行：`技能信息 {skill_name}` → enter
- 440行：技能背包 上一页/下一页 → enter（`跳转[页码]` 保留 input）

### 4. `Game_main/g6_dungeon.py`（副本系统，31 处改 enter）

- 副本列表 (654/658/667行)：`副本信息 {id}`、`挑战副本 {id}`、翻页 → enter（`跳转【页数】` 保留）
- 副本信息页 (750行)：`挑战副本 {id}`、副本列表、收回 → enter
- 怪物列表 (803/809行)：`挑战怪物 {index}`、放弃副本、当前角色、收回 → enter
- 未出战提示 (918/980/1075/1144/1269行)：角色背包、收回 → enter（`出战` 保留 input）
- 错误/提示页 (960/967/1124/1131/1709/1719/1747/1761行)：副本列表、挑战副本 → enter（`副本信息` 裸指令保留 input；1187/1229行 `挑战副本` show带示例"挑战副本 1"保留 input）
- 挑战流程 (1090/1106/1109/1217/1247行)：查看怪物、放弃副本、`副本信息 {id}`、`挑战副本 {id}`、`挑战怪物 1` → enter（1253行 `挑战怪物` 裸指令保留）
- 掉落展示 (1594行)：`卷轴信息 {item_name}` → enter
- 战斗结算 (1685/1687/1691行)：副本列表、战斗记录、查看怪物、放弃副本、`挑战副本 {dungeon_id}` → enter

### 5. `Game_main/g7_equip.py`（装备系统，40 处改 enter + 1 处锚点修复）

- 装备背包 (521/523/528/532行)：`卸下装备 {部位}`、`穿戴装备 {id}`、`装备详情 {id}`、`强化装备 {id}`、`出售装备 {id}`、翻页、当前装备、当前角色、角色背包 → enter（`跳转[页码]` 保留）
- 当前装备 (614行)：装备背包、角色背包、收回 → enter
- 穿戴/卸下结果 (678/728行)：当前装备、装备背包 → enter
- 强化结果 (786/788行)：`强化装备 {id}`、`装备详情 {id}`、当前装备 → enter
- 出售结果 (818行)：装备背包、当前装备 → enter
- 装备详情 (890/892行)：`穿戴装备 {id}`、`卸下装备 {部位}`、`强化装备 {id}`、`出售装备 {id}`、装备背包 → enter
- 各类错误/提示页 (919/937/959/989/1011/1032/1047/1060/1074/1149/1165/1186/1197/1247/1269/1290/1300/1318/1406/1419/1454/1476/1497/1507行)：装备背包、角色背包、当前装备、当前角色、帮助、副本列表、`卸下装备 武器`、`强化装备 1`、`装备详情 1`、`装备详情 {id}` → enter（`出战`、`装备详情` 裸指令保留 input）

**⚠️ 联动修复（1378/1379/1385/1386行）：** 强化保底进度插入逻辑用 `markdown_content.replace("\n***\n<qqbot-cmd-input", ...)` 做字符串锚定，按钮改为 enter 后锚点同步改为 `<qqbot-cmd-enter`，否则保底进度提示会静默失效。

### 6. `Game_main/g8_power.py`（战力系统，11 处改 enter）

- 42行：角色背包 → enter（`出战` 保留）
- 71行：战力排行、装备背包、查看本源 → enter
- 84-90行：`战力排行 全服/萧炎/王林/韩立/石昊/叶凡/孟川` ×7 → enter
- 115/145行：战力排行、我的战力 → enter

### 7. `Game_main/g9_yaoyuan.py`（药园炼丹，31 处改 enter + 1 处 bug 修复）

- 药园总览 (946/948/952/953行)：`采摘 {田号}`、`施肥 {田号}`、一键采摘、种子背包、种子商店 → enter（`播种 `/`解锁药田 `/`施肥 ` 带*保留）
- 种子商店 (1012/1017/1018/1019行)：翻页、种子背包、`种子商店 斗破苍穹/仙逆` → enter（`丹方列表 ` 尾空格保留）
- **🐛 bug 修复（1012行）：** 原代码 `text` 与 `show` 写反——`<qqbot-cmd-input text='购买×1' show='购买种子 {seed_name}-1' />`，点击后会把无效文本"购买×1"插入输入框。已修复为 `<qqbot-cmd-enter text='购买种子 {seed_name}-1' />`（×1/×5 两个按钮），点击直接完成购买
- 种子背包/购买/出售 (1046/1056/1105/1162行)：种子商店、物品背包、种子背包 → enter（`一键播种 `/`出售药材 `/`播种 ` 保留）
- 播种/采摘/解锁/施肥 (1221/1284/1336/1401/1448/1490行)：药园、种子背包、`采摘 {田号}` → enter
- 丹炉总览 (1536/1539/1542/1545行)：`收丹 {炉号}`、`加速炼丹 {炉号}`、`添火 {uid}-{炉号}`、一键收丹、丹方列表、添火次数 → enter（`炼丹 `/`收丹 `/`解锁丹炉 `/`加速炼丹 `/`添火 ` 保留）
- 丹方列表 (1603/1610/1611行)：翻页、查看丹炉 → enter（`炼丹 {丹方名}-` 缺炉号、`炼丹 ` 保留）
- 炼丹/收丹/服丹/解锁/添火/加速 (1706/1762/1835/1978/2042/2228/2268行)：查看丹炉、一键收丹、当前角色、物品背包、添火次数、`收丹 {炉号}` → enter

### 8. `func/pd_func.py`（装饰器，6 处改 enter）

- 34-39行：未选初始角色提示中的 `角色介绍 萧炎/王林/韩立/石昊/叶凡/孟川` ×6 → enter
- 42行：`选择角色 `/`角色介绍 `（尾空格+*）保留 input

### 9. `Tool/tool_command.py`（快捷指令，2 处改 enter）

- 118行 `all_write_command()`：快捷指令按钮 → `<qqbot-cmd-enter text="{cmd}" />`
- 161-162行 `all_write_cmd()`：`flag=0`（无后缀）→ enter；`flag=1`（需后缀参数，show带*）保留 input

### 10. `claude_md/CLAUDE.md`（规范文档）

- "Response Format Convention" 一节更新：补充两种按钮格式的使用规则、enter 仅支持 text 属性、enter 群聊不可用的注意事项

## 四、未修改的文件说明

- `claude_md/药田炼丹实施方案.md`、`claude_md/药园炼丹系统_世界观适配方案.md`：历史设计文档，内容为方案示例，不影响运行，保持原样
- `main.py`、`bot_main.py`、`output_main.py`、`Game_main/g2_canwu.py`、`Game_main/g4_benyuan.py`、`Tool/combat_system.py`、`Tool/tool_user.py`：不含按钮标签，无需修改

## 五、修改统计与校验

| 文件 | 改为 enter 数量 |
|------|------|
| g0_menu.py | 39 |
| g7_equip.py | 40 |
| g6_dungeon.py | 31 |
| g9_yaoyuan.py | 31 |
| g1_role.py | 21 |
| g8_power.py | 11 |
| pd_func.py | 6 |
| g5_skill.py | 2 |
| tool_command.py | 2 |
| **合计** | **183** |

- ✅ 全部 9 个修改的 .py 文件通过 `py_compile` 语法校验
- ✅ 全项目复查：剩余 `qqbot-cmd-input` 均为需玩家输入参数的指令（尾空格参数位 / show带*或示例 / 必须带参裸指令）

## 六、注意事项

1. **群聊限制：** `qqbot-cmd-enter` 官方仅在单聊（C2C）生效，群聊和文字子频道中按钮不可点击。玩家群聊中仍可手动输入指令，功能不受影响；若需群聊可用按钮，需改回 `qqbot-cmd-input`
2. **按钮文案：** enter 不支持 `show`，按钮显示为指令本身（如 `/主菜单`），原 emoji 文案（如 `⬅️ 返回主菜单`）不再显示
3. **text 长度：** enter 的 text 限制 100 字符，当前所有指令均远低于此限制
