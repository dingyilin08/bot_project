# 问道诸天 QQ机器人 - 宝塔面板部署指南

## 一、环境要求

| 组件 | 版本要求 |
|------|---------|
| 操作系统 | CentOS 7+ / Ubuntu 18.04+ / Debian 10+ |
| 宝塔面板 | 最新版 |
| Python | 3.11+ |
| MySQL | 5.7+ |
| Nginx | 1.18+（用于反向代理SSL） |

## 二、宝塔面板环境准备

### 2.1 安装必要软件

在宝塔面板「软件商店」中安装：

1. **MySQL 5.7** — 数据库服务
2. **Nginx** — 反向代理 + SSL 终端
3. **Python项目管理器 2.x** — Python项目托管

### 2.2 安装 Python 3.11+

```bash
# SSH登录服务器，安装Python 3.11
# CentOS:
sudo yum install -y python311

# Ubuntu:
sudo apt install -y python3.11 python3.11-venv python3.11-dev
```

## 三、项目部署步骤

### 3.1 上传项目文件

将整个 `Bot_project` 目录上传到服务器，例如 `/www/wwwroot/bot_project/`

目录结构确认：
```
/www/wwwroot/bot_project/
├── main.py                  # 生产入口
├── bot_main.py              # WebSocket入口（可选）
├── output_main.py           # 指令路由
├── requirements.txt         # 依赖清单
├── Game_main/               # 游戏模块
├── Tool/                    # 工具模块
├── sql/                     # 数据库连接
├── func/                    # 装饰器
├── images/                  # 静态资源
└── 数据库源文件/
    └── bot_project.sql
```

### 3.2 创建虚拟环境并安装依赖

```bash
cd /www/wwwroot/bot_project

# 创建虚拟环境
python3.11 -m venv venv

# 激活虚拟环境
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt

# 如果 botpy 是本地库，手动安装
# pip install /path/to/botpy
```

### 3.3 初始化数据库

#### 3.3.1 创建数据库

在宝塔面板「数据库」中：
1. 点击「添加数据库」
2. 数据库名：`bot_project`
3. 用户名：`bot_project`
4. 密码：设置一个强密码
5. 访问权限：`本地服务器`

#### 3.3.2 导入数据

```bash
# 方式一：通过宝塔面板
# 数据库 → 管理 → 导入 → 选择 bot_project.sql

# 方式二：命令行导入
mysql -u bot_project -p bot_project < /www/wwwroot/bot_project/数据库源文件/bot_project.sql
```

#### 3.3.3 修改数据库连接配置

编辑 [sql/mysql.py](file:///d:/Users/76384/Desktop/Bot_project/sql/mysql.py) 中的 `DB_CONFIG`：

```python
DB_CONFIG = {
    "host": "127.0.0.1",        # 改为本地数据库地址
    "port": 3306,                # 改为你的MySQL端口
    "user": "bot_project",      # 数据库用户名
    "password": "你的密码",      # 数据库密码
    "db": "bot_project",        # 数据库名
    "charset": "utf8mb4",
    "autocommit": False
}
```

### 3.4 配置 main.py

编辑 [main.py](file:///d:/Users/76384/Desktop/Bot_project/main.py)，确认以下配置：

```python
# QQ Bot 配置
APP_ID = "102135148"                              # 你的AppID
BOT_SECRET = "你的Bot Secret"                      # 你的Bot Secret
SANDBOX_MODE = False                               # 生产环境设为 False

# 启动配置（最后几行）
uvicorn.run(app,
    host="127.0.0.1",          # 只监听本地，通过Nginx代理
    port=8000,                 # 内部端口
    log_level="info"
    # 注意：SSL由Nginx处理，这里不需要ssl_keyfile和ssl_certfile
)
```

### 3.5 配置 Nginx 反向代理

在宝塔面板「网站」→「添加站点」：
1. 域名：`iot-device.icu`（你的域名）
2. 创建后进入「设置」→「配置文件」

替换为以下配置：

```nginx
server {
    listen 80;
    server_name iot-device.icu;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name iot-device.icu;

    # SSL证书（在宝塔面板中申请Let's Encrypt免费证书）
    ssl_certificate     /www/server/panel/vhost/cert/iot-device.icu/fullchain.pem;
    ssl_certificate_key /www/server/panel/vhost/cert/iot-device.icu/privkey.pem;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    # 静态资源
    location /images/ {
        alias /www/wwwroot/bot_project/images/;
        expires 30d;
    }

    location /102135148.json {
        alias /www/wwwroot/bot_project/images/102135148.json;
    }

    # Webhook和API反向代理到FastAPI
    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Bot-Appid $http_x_bot_appid;
        proxy_read_timeout 60s;
    }
}
```

### 3.6 配置 SSL 证书

在宝塔面板「网站」→「设置」→「SSL」：
1. 选择「Let's Encrypt」
2. 勾选域名，点击「申请」
3. 开启「强制HTTPS」

### 3.7 配置 Python 项目管理器

在宝塔面板「软件商店」→「Python项目管理器 2.x」：

1. 点击「添加项目」
2. 填写：
   - **项目名称：** bot_project
   - **项目路径：** `/www/wwwroot/bot_project`
   - **启动文件：** `main.py`
   - **Python版本：** 选择 Python 3.11
   - **端口：** 8000
   - **绑定IP：** 127.0.0.1
3. 点击「确定」，然后「启动」

### 3.8 配置开机自启

在 Python 项目管理器中，勾选项目的「开机自启」选项。

或者使用 Supervisor 方式：

```bash
# 在宝塔「软件商店」安装 Supervisor
# 添加守护进程：
# 名称：bot_project
# 启动命令：/www/wwwroot/bot_project/venv/bin/python /www/wwwroot/bot_project/main.py
# 进程目录：/www/wwwroot/bot_project
```

## 四、QQ 开放平台配置

登录 [QQ 开放平台](https://q.qq.com/)：

1. 进入你的机器人应用
2. 「开发设置」→「消息配置」
3. Webhook 地址填写：`https://iot-device.icu/webhook`
4. 点击「验证」确认连接成功
5. 保存配置

## 五、验证部署

### 5.1 健康检查

```bash
# 检查服务是否正常运行
curl -k https://iot-device.icu/health

# 应返回：{"status": "healthy"}
```

### 5.2 查看日志

```bash
# Python项目管理器日志
tail -f /www/wwwroot/bot_project/logs/*.log

# 或通过宝塔面板查看
# 软件商店 → Python项目管理器 → 日志
```

### 5.3 QQ群内测试

在QQ群中 @机器人 发送：
- `菜单` — 查看主菜单
- `帮助` — 查看帮助信息

## 六、常见问题

### Q1: 启动失败，提示端口占用

```bash
# 查看端口占用
netstat -tlnp | grep 8000

# 杀掉占用进程
kill -9 <PID>
```

### Q2: 数据库连接失败

检查：
1. MySQL 是否启动：`systemctl status mysqld`
2. 数据库用户权限：`GRANT ALL ON bot_project.* TO 'bot_project'@'127.0.0.1';`
3. 防火墙是否放行 MySQL 端口

### Q3: Webhook 验证失败

1. 确认 Nginx 反向代理配置正确
2. 确认 `SANDBOX_MODE` 设置正确
3. 检查 `BOT_SECRET` 是否与QQ开放平台一致
4. 查看服务器日志排查签名错误

### Q4: 使用 bot_main.py 作为入口

如果想要使用 WebSocket 方式（botpy SDK），需要：
1. 上传并安装你的 botpy 本地库到虚拟环境
2. 创建 `config.yaml` 配置文件（放在项目根目录）：

```yaml
appid: "102135148"
secret: "你的Bot Secret"
```

3. 启动命令改为：`python bot_main.py`

## 七、日常维护

### 更新代码

```bash
cd /www/wwwroot/bot_project
git pull                    # 如果用Git管理
# 重启服务
# 在宝塔Python项目管理器中点击「重启」
```

### 数据库备份

在宝塔面板「计划任务」中添加：
- **任务类型：** 备份数据库
- **数据库：** bot_project
- **执行周期：** 每天凌晨 3:00
- **保留份数：** 7

### 日志清理

```bash
# 定期清理旧日志
find /www/wwwroot/bot_project -name "*.log" -mtime +30 -delete
```