# 《诸天灵契》灵兽子系统接入规划

## 1. 接入目标

本规划把《灵兽玩法全面重制设计文档》拆成可灰度、可回滚的工程阶段，并先完成最小可靠底座：

- 一名角色拥有一个独立灵兽位；
- 一只灵兽最多绑定一名角色；
- 灵兽战力计入其绑定角色，并进入当前角色总战力与排行榜；
- 副本、队伍战斗、轮海深渊按实际参战角色读取灵兽；
- 角色切换只切换生效对象，不破坏其他角色的绑定；
- QQ Markdown 菜单、文本交互、角色页和战力页提供完整入口。

完整重制继续采用“寻踪—结契—养成—觉醒—灵阵—赛季”的长期路线，但每个阶段必须独立可用，不能依赖尚未发布的下一阶段。

## 2. 当前基线与本期范围

现有系统已有四类灵兽、每日寻访、灵兽园容量、PVE 快照及少量本源协同。主要缺口是 `is_active` 代表账号全局唯一灵兽，无法为不同角色保存配置，且灵兽未进入战力分项。

本期 P0 已接入：

1. `equipped_role_id` 角色绑定；
2. `(uid, equipped_role_id)` 唯一约束；
3. `power_beast` 战力缓存分项；
4. 统一灵兽资质、羁绊、灵契战力规则；
5. 角色属性页、我的战力、灵兽园和菜单展示；
6. 副本、队伍、深渊按参战角色冻结灵兽快照；
7. 旧 `is_active` 数据兼容与迁移。

本期不伪装实现喂养、洗髓、血脉、技能书、三灵兽灵阵和世界寻踪；这些功能按后续阶段接入。

## 3. 领域边界

建议按以下职责组织代码：

| 层级 | 模块 | 职责 |
| --- | --- | --- |
| 纯规则 | `Game_domain/spirit_beast_rules.py` | 资质归一化、羁绊等级、灵契强度、战力公式 |
| 玩家入口 | `Game_main/g12_spirit_beast.py` | 灵兽园、寻访、绑定、展示与战斗快照 |
| 战力聚合 | `Tool/tool_power.py` | 查询角色绑定、计算并写回 `power_beast` |
| 角色生命周期 | `Game_main/g1_role.py` | 出战/收回时同步当前生效标记，角色页展示 |
| 命令路由 | `output_main.py` | 保留“灵兽编号 角色编号”之间的分隔符 |
| 战斗消费者 | 副本、队伍、深渊服务 | 显式传递参战 `role_id`，只消费冻结快照 |

规则模块不得连接数据库；战斗模块不得自行复制资质公式；玩家文本不得直接解释为战斗数值。

## 4. 数据模型与迁移

### 4.1 本期字段

`user_spirit_beast` 新增：

- `equipped_role_id INT NULL`：绑定角色编号；
- 唯一键 `uk_spirit_beast_role(uid, equipped_role_id)`：同一玩家的一名角色只能有一只灵兽。

`user_zt` 新增：

- `power_beast BIGINT NOT NULL DEFAULT 0`：当前出战角色的灵兽战力缓存。

`is_active` 暂不删除，只作为“当前出战角色所绑定灵兽”的兼容标记。真实归属始终读取 `equipped_role_id`。

### 4.2 旧数据迁移

执行 `数据库源文件/p7_role_spirit_beast.sql`：

1. 幂等新增字段；
2. 把旧 `is_active=1` 灵兽绑定给当时的出战角色；
3. 清理极端重复绑定；
4. 建立唯一键；
5. 新增战力分项；
6. 按当前角色重新同步 `is_active`。

发布顺序必须是“备份数据库 → 执行迁移 → 发布代码 → 冒烟验证”。迁移可重放，不包含玩家资产删除。

## 5. 角色绑定规则

### 5.1 指令

- `灵兽出战 <灵兽编号>`：绑定到当前出战角色；
- `灵兽出战 <灵兽编号> <角色编号>`：绑定到指定自有角色。

### 5.2 事务流程

1. `FOR UPDATE` 锁定目标灵兽；
2. 校验灵兽属于当前玩家；
3. 锁定并校验目标角色属于当前玩家；
4. 解除“目标角色原灵兽”与“目标灵兽旧角色”绑定；
5. 写入新 `equipped_role_id`；
6. 若目标角色当前出战，同步 `is_active=1`；
7. 刷新当前角色总战力；
8. 所有写操作成功后统一提交。

任何校验失败都不得产生部分写入。数据库唯一键负责抵御并发下的双重绑定。

## 6. 灵兽战力

本期公式：

`灵兽战力 = 500 + 资质×20 + 羁绊等级×120 + 灵契强度×100`

其中：

- 资质统一限制在60—100；
- 羁绊每100经验1级，最多按10级计算；
- 灵契强度与真实战斗加成共用函数；
- 输出、护法、灵医、扰敌采用同一估值，不制造定位歧视。

总战力变为：

`基础 + 等级 + 装备 + 本源 + 技能 + 灵兽`

刷新时机包括灵兽绑定、角色出战、角色收回和玩家主动查看战力。未来喂养、突破、洗髓、血脉或技能变化也必须调用同一刷新入口。

## 7. 战斗接入

副本、队伍与深渊创建战斗时必须传入真实 `role_id`：

1. 查询该角色的 `equipped_role_id` 绑定；
2. 生成只包含白名单字段的灵兽快照；
3. 把快照写入战斗实体或持久化会话；
4. 后续回合只读快照，不再查询实时灵兽；
5. 战斗结束后，新战斗才读取最新绑定。

这样可防止中途换兽篡改战局，也支持队伍中每名玩家按自己的参战角色携带不同灵兽。

## 8. QQ 交互与菜单

### 8.1 灵兽菜单

首屏只保留：我的灵兽、灵兽图鉴、每日寻访、绑定当前角色、洞府容量、主菜单。说明文本明确“一角色一灵兽”。

### 8.2 灵兽园

每只灵兽展示：编号、名称、定位、资质、性格、血脉、灵契、战力、绑定角色。底部提供“灵兽出战 灵兽编号”的文本交互入口。

### 8.3 角色与战力页

- 角色属性显示随行灵兽、资质、羁绊等级和战力；
- 我的战力显示灵兽战力数值与占比；
- 两页均提供可点击的“灵兽”入口。

Markdown 保持短段落、单层列表和明确动词；需要玩家补编号的按钮使用尾随空格，避免伪装成一键执行。

## 9. 后续分期

### P1：寻踪与重复转化

- 六界寻踪、三个线索选择、鉴灵保底；
- 重复灵兽转精魄、派遣或保留；
- 迁移捕获记录为可审计的保底计数。

### P2：基础养成

- 等级、境界、喂养、照料、羁绊故事；
- 洞府灵兽园产出御兽灵息；
- 养成继承与重置返还。

### P3：资质、血脉与技能

- 四维资质、洗髓确认、连续未提升保护；
- 六节点血脉与返祖；
- 灵契书库、无损装卸与技能预设。

### P4：三位灵阵

- 主契、护契、辅契；
- 灵体值、条件触发、战报压缩；
- 六界共鸣与混合灵阵补偿。

### P5：长期内容

- 万灵秘境、宗门护山灵兽、周记与赛季目标；
- 六位主角专属协同；
- 化形、图鉴故事、外观与灵兽园布景。

每期都要先发布迁移，再发布兼容代码；旧战斗快照至少兼容一个版本周期。

## 10. 测试与验收

### 单元测试

- 资质上下界、羁绊等级上限、四定位同权；
- 指令支持单编号、空格双编号与短横线双编号；
- 未绑定返回0战力；
- 非当前角色的旧版 `is_active` 不得被重复计入。

### 集成测试

- 同一角色更换灵兽后只保留一个绑定；
- 同一灵兽从角色A移动到角色B后，A不再获得战力；
- 切换角色后 `power_beast` 与角色页一致；
- 收回角色后总战力清零，但绑定仍保存；
- 副本、队伍和深渊快照读取对应角色灵兽；
- 旧库缺字段时只走兼容读取，不产生500错误。

### 上线冒烟

1. 执行迁移两次均成功；
2. 老玩家原出战灵兽仍绑定原角色；
3. 使用“灵兽出战 灵兽编号 角色编号”成功；
4. 查看角色属性与我的战力数值一致；
5. 开启一场副本和一场深渊，战报出现正确灵契；
6. 切换角色后，下一场战斗使用新角色自己的灵兽。

## 11. 监控与回滚

重点记录绑定失败、唯一键冲突、快照缺失、战力刷新异常和迁移1054错误。上线后观察灵兽绑定成功率、带兽开战率与战力刷新耗时。

若应用异常，可回滚代码至旧版：`is_active` 仍被维护，旧版仍可工作。数据库新增列和唯一键先保留，不在紧急回滚中删除；修复后再次发布即可。任何回滚都不得删除灵兽、羁绊或玩家战力历史数据。
